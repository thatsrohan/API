export class product{
    constructor(public id:string,
        public title:string,
        public description:string,
        public price:number) 
}
// if you parameterize the constructor variables as public then it automatically becomes the class's attributes
